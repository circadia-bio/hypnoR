% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_hypnogram.R
\name{read_hypnogram}
\alias{read_hypnogram}
\title{Read a hypnogram from file}
\usage{
read_hypnogram(
  path,
  format = "auto",
  epoch_sec = 30L,
  tz = "UTC",
  quiet = FALSE
)
}
\arguments{
\item{path}{Path to the hypnogram file.}

\item{format}{One of \code{"csv"}, \code{"edf_annotations"}, \code{"yasa"},
\code{"compumedics"}, \code{"nox"}, or \code{"auto"} (default).  When \code{"auto"}, the
format is inferred from the file extension and header.}

\item{epoch_sec}{Epoch duration in seconds.  Default \code{30}.}

\item{tz}{Time zone string passed to \code{\link[lubridate:as_datetime]{lubridate::as_datetime()}}.
Default \code{"UTC"}.}

\item{quiet}{Suppress informational messages.  Default \code{FALSE}.}
}
\value{
A tibble with columns:
\describe{
\item{epoch}{Integer epoch index (1-based).}
\item{time}{\code{POSIXct} timestamp for the start of each epoch.}
\item{stage}{Ordered factor.  Level set depends on staging resolution:
full AASM (\code{W}, \code{N1}, \code{N2}, \code{N3}, \code{REM}) or coarse actigraphy-derived
(\code{W}, \code{Sleep}, \verb{Quiet sleep}).}
\item{source}{Character — the originating device / scorer label, if
available in the file header.}
}
}
\description{
Imports a staged hypnogram from common formats and returns a tidy tibble
with one row per epoch.  The resulting object is the standard input for all
other \code{hypnoR} functions.
}
\details{
\code{hypnoR} distinguishes two staging resolutions:
\itemize{
\item \strong{Full AASM} (5-state): supplied by \code{mrpheus}.
\item \strong{Coarse} (3-state): supplied by \code{zeitR}.
}

All downstream metric functions are resolution-agnostic: they compute what
is possible given the available stages and document which metrics require
full AASM staging.
}
\examples{
\dontrun{
hyp <- read_hypnogram("night_001.csv")
hyp <- read_hypnogram("night_001.edf", format = "edf_annotations")
}
}
