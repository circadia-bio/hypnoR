% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_hypnogram.R
\name{window_hypnogram}
\alias{window_hypnogram}
\title{Restrict a hypnogram to a time or epoch window}
\usage{
window_hypnogram(
  hypnogram,
  lights_off = NULL,
  lights_on = NULL,
  from_epoch = NULL,
  to_epoch = NULL
)
}
\arguments{
\item{hypnogram}{A \code{hypnor_hypnogram} object as returned by
\code{\link[=new_hypnogram]{new_hypnogram()}} or \code{\link[=read_hypnogram]{read_hypnogram()}}, or any data frame with at
minimum \code{epoch} and \code{stage} columns -- it will be passed through
\code{\link[=new_hypnogram]{new_hypnogram()}} automatically if not already a \code{hypnor_hypnogram}.}

\item{lights_off, lights_on}{\code{POSIXct} or \code{NULL}. Both must be supplied
together. Requires \code{hypnogram} to carry real timestamps (i.e.
\code{start_time} was supplied to \code{\link[=new_hypnogram]{new_hypnogram()}} or
\code{mrpheus::export_hypnogram()}) -- errors if \code{time} is entirely \code{NA}.
Mutually exclusive with \code{from_epoch}/\code{to_epoch}.}

\item{from_epoch, to_epoch}{Integer or \code{NULL}. Either may be supplied
alone (an open-ended window); defaults to the first/last epoch in
\code{hypnogram} respectively. Mutually exclusive with
\code{lights_off}/\code{lights_on}.}
}
\value{
A \code{hypnor_hypnogram}, filtered to the window, with \code{epoch_sec}
and \code{resolution} carried over unchanged from the input -- \code{resolution}
is \emph{not} re-detected from the (possibly much smaller) windowed subset,
since a short window could plausibly contain no REM/N3 epochs and get
misdetected as \code{"coarse"} otherwise.
}
\description{
Filters a hypnogram down to a sub-range, either by clock time
(\code{lights_off}/\code{lights_on}) or by epoch number (\code{from_epoch}/\code{to_epoch}),
re-attaching \code{epoch_sec} and \code{resolution} correctly on the result. This
is the single place windowing logic lives in hypnoR -- \code{\link[=compute_cycles]{compute_cycles()}}
and \code{\link[=compute_transitions]{compute_transitions()}} have no \code{lights_off}/\code{lights_on} arguments of
their own; window first, then pass the windowed hypnogram in.
}
\details{
\code{\link[=compute_sleep_architecture]{compute_sleep_architecture()}}'s own \code{lights_off}/\code{lights_on} arguments
are sugar for calling this first: passing them restricts \code{TST}, \code{SOL},
\code{WASO}, and every other metric to the window, not just the \code{TIB}/\code{SE}
denominator.
}
\examples{
\dontrun{
hyp <- new_hypnogram(mrpheus_hyp)

# By clock time
hyp_night <- window_hypnogram(
  hyp,
  lights_off = as.POSIXct("2024-01-01 23:00:00", tz = "UTC"),
  lights_on  = as.POSIXct("2024-01-02 07:00:00", tz = "UTC")
)

# By epoch range (e.g. isolating a sleep period out of a longer recording)
hyp_sleep <- window_hypnogram(hyp, from_epoch = 1001L, to_epoch = 1950L)

compute_cycles(hyp_sleep)
compute_transitions(hyp_sleep)
}
}
