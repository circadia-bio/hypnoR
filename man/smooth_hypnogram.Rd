% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth.R
\name{smooth_hypnogram}
\alias{smooth_hypnogram}
\title{Smooth a hypnogram by reassigning short, likely-spurious runs}
\usage{
smooth_hypnogram(hypnogram, method = "aasm_isolated", min_run_epochs = 2L)
}
\arguments{
\item{hypnogram}{A \code{hypnor_hypnogram} object as returned by
\code{\link[=new_hypnogram]{new_hypnogram()}} or \code{\link[=read_hypnogram]{read_hypnogram()}}, or any data frame with at
minimum \code{epoch} and \code{stage} columns -- it will be passed through
\code{\link[=new_hypnogram]{new_hypnogram()}} automatically if not already a \code{hypnor_hypnogram}.}

\item{method}{One or more of \code{"aasm_isolated"} (default) and
\code{"min_run"}. When both are requested, \code{"aasm_isolated"} is always
applied first, then \code{"min_run"} -- regardless of the order the two
are given in \code{method}:
\describe{
\item{\code{"aasm_isolated"}}{A single epoch whose stage differs from
both of its immediate neighbours is reassigned to the
neighbours' stage, but only when both neighbours share the
\emph{same} stage. This mirrors the conservative manual-scoring
convention of resolving an ambiguous single epoch bounded
identically on both sides. It never touches a run of 2+ epochs,
and never touches an isolated epoch whose two neighbours
disagree with each other.}
\item{\code{"min_run"}}{Any run shorter than \code{min_run_epochs} is
reassigned to whichever flanking run is longer (ties favour the
preceding run). Broader than \code{"aasm_isolated"}: it does not
require the two flanking runs to agree with each other, and
(with \code{min_run_epochs > 2}) can merge runs longer than a single
epoch. Applied as a single left-to-right pass over the
run-length structure computed at the start of this step -- it
is not iterated to a fixed point, so a pathological hypnogram
may still contain runs shorter than \code{min_run_epochs} afterwards.
Call \code{smooth_hypnogram()} again on the result for a second pass
if needed. Note that if \code{"aasm_isolated"} ran first, it may
already have merged some runs together, which changes the
flanking-run lengths \code{"min_run"} sees -- the two methods are
genuinely sequential, not independent.}
}}

\item{min_run_epochs}{Only used by \code{"min_run"}. Minimum run length (in
epochs) to leave untouched. Default \code{2L} (merges only single-epoch
runs).}
}
\value{
The input \code{hypnor_hypnogram}, with two changes: \code{stage} now
holds the smoothed labels, and a new \code{stage_raw} column preserves the
original, unsmoothed labels for comparison/audit. All other columns
and attributes (\code{epoch_sec}, \code{resolution}) are unchanged.
}
\description{
Raw automatic sleep staging is typically a per-epoch classification with
no temporal continuity constraint (e.g. \code{mrpheus::stage_epochs()} is a
pure per-epoch argmax over posterior probabilities), which can produce
brief, isolated stage flips inconsistent with sleep physiology -- a
single REM epoch nested inside an N2 run, for instance.
\code{smooth_hypnogram()} offers two independent, rule-based cleanup passes
that operate purely on stage labels (no posterior probabilities
required, and none are carried through by \code{\link[=new_hypnogram]{new_hypnogram()}} in any
case).
}
\examples{
\dontrun{
hyp        <- new_hypnogram(mrpheus_hyp)
hyp_smooth <- smooth_hypnogram(hyp)
mean(hyp_smooth$stage != hyp_smooth$stage_raw)  # proportion of epochs changed

# Broader cleanup: also merge non-isolated short runs
smooth_hypnogram(hyp, method = c("aasm_isolated", "min_run"), min_run_epochs = 3)
}
}
