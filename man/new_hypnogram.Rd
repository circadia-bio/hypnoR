% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/new_hypnogram.R
\name{new_hypnogram}
\alias{new_hypnogram}
\alias{new_hypnogram.default}
\alias{new_hypnogram.mrpheus_hypnogram}
\title{Construct a hypnoR hypnogram object}
\usage{
new_hypnogram(x, ...)

\method{new_hypnogram}{default}(
  x,
  epoch_sec = NULL,
  resolution = NULL,
  subject_id = NULL,
  source = NULL,
  ...
)

\method{new_hypnogram}{mrpheus_hypnogram}(
  x,
  epoch_sec = NULL,
  resolution = NULL,
  subject_id = NULL,
  source = NULL,
  ...
)
}
\arguments{
\item{x}{A data frame with at minimum \code{epoch} and \code{stage} columns, or an
object with a dedicated \code{new_hypnogram()} method (currently:
\code{mrpheus_hypnogram}). Recognised optional columns: \code{time} (\code{POSIXct}),
\code{subject_id}, \code{source}.}

\item{...}{Passed to methods; currently unused.}

\item{epoch_sec}{Epoch duration in seconds. When \code{x} carries its own
epoch duration (e.g. an \code{mrpheus_hypnogram}'s \code{epoch_s} attribute) that
value is used unless \code{epoch_sec} is explicitly supplied here. Falls
back to \code{30L} when no value can be determined from either source.}

\item{resolution}{\code{"aasm"}, \code{"coarse"}, or \code{NULL} (default). When \code{NULL},
resolution is auto-detected from the stage labels present in \code{x} (or,
for \code{mrpheus_hypnogram} input, taken from its \code{resolution} attribute).}

\item{subject_id}{Character or \code{NULL}. Overrides any subject/participant
identifier carried by \code{x}.}

\item{source}{Character or \code{NULL}. Overrides any source/scorer label
carried by \code{x}.}
}
\value{
A tibble of class \code{hypnor_hypnogram} with columns \code{epoch}
(integer), \code{time} (\code{POSIXct}, \code{NA} if unknown), \code{stage} (ordered
factor), \code{subject_id} (character, \code{NA} if unknown), and \code{source}
(character, \code{NA} if unknown). The detected \code{epoch_sec} and
\code{resolution} are attached as attributes.
}
\description{
\code{new_hypnogram()} is the low-level constructor for the \code{hypnor_hypnogram}
class used throughout \code{hypnoR}. It normalises hypnogram-shaped input --
whether a bare tibble (as produced by \code{zeitR::export_hypnogram()} or
\code{\link[=read_hypnogram]{read_hypnogram()}}) or a specially classed object (currently:
\code{mrpheus::export_hypnogram()}'s \code{mrpheus_hypnogram}) -- into the single
internal representation that every other \code{hypnoR} function expects.
}
\examples{
\dontrun{
# From a bare tibble (e.g. zeitR::export_hypnogram() output)
new_hypnogram(zeitr_hyp)

# From mrpheus::export_hypnogram() output
new_hypnogram(mrpheus_hyp)
}
}
